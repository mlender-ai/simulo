# Anthropic Claude API Key (필수 — 설정 페이지에서도 입력 가능)
ANTHROPIC_API_KEY=sk-ant-api03-...

# PostgreSQL 연결 URL (선택 — 없으면 localStorage만 사용)
DATABASE_URL=postgresql://user:password@host:5432/simulo

# 관리자 비밀번호 (선택)
ADMIN_PASSWORD=changeme
