# QA Report — 2026-04-15

## 자동 검사 요약
| 항목 | 상태 |
|------|------|
| 빌드 | ⚠️ FAIL |
| 린트 | ✅ PASS |
| 타입 체크 | ❌ FAIL |
| E2E 테스트 | ✅ PASS |

---

## 발견된 이슈

### CRITICAL (즉시 수정 필요)

#### 1. **TypeScript 모듈 해석 실패**
- **위치**: `playwright.config.ts`, `e2e/settings.spec.ts`
- **문제**: `@playwright/test` 모듈을 찾을 수 없음 (TS2307)
- **영향**: 빌드 파이프라인 실패, IDE 타입 지원 불가
- **원인**: 
  - `@playwright/test` 타입 선언 누락
  - `package.json`에 모듈 미등재 또는 버전 호환성 문제
- **해결 방안**:
  ```bash
  npm install --save-dev @playwright/test@latest
  npm install --save-dev @types/node
  ```
- **검증**: `tsconfig.json`에서 `skipLibCheck: false` 확인

#### 2. **암묵적 `any` 타입 정의**
- **위치**: `e2e/settings.spec.ts` (라인 4, 12, 22)
- **문제**: 함수 매개변수 `page`가 명시적 타입 선언 없음 (TS7031)
- **영향**: 타입 안정성 저하, 자동완성 불가능
- **수정 예시**:
  ```typescript
  // ❌ 현재
  test('description', async ({ page }) => {
  
  // ✅ 개선
  test('description', async ({ page }: { page: Page }) => {
  ```

---

### HIGH (빠른 수정 권장)

#### 3. **빌드 프로세스 불일치**
- **문제**: `tsconfig.json`이 Playwright 설정과 동기화되지 않음
- **영향**: 로컬 개발 환경과 CI/CD 환경 간 테스트 결과 차이 가능성
- **수정**:
  ```json
  // tsconfig.json
  {
    "compilerOptions": {
      "skipLibCheck": true,
      "moduleResolution": "node",
      "resolveJsonModule": true,
      "types": ["@playwright/test", "node"]
    }
  }
  ```

#### 4. **대용량 컴포넌트 파일 (823~893줄)**
- **위치**: `lib/claude.ts` (893줄), `components/InputSection.tsx` (824줄), `components/ReportTabs.tsx` (811줄)
- **문제**: 단일 책임 원칙(SRP) 위반, 유지보수성 저하
- **영향**: 테스트 작성 어려움, 변경 시 부작용 위험 증가

---

### MEDIUM (개선 권장)

#### 5. **E2E 테스트 타입 안정성 미흡**
- **현황**: E2E 테스트 3개 파일 (`api.spec.ts`, `pages.spec.ts`, `settings.spec.ts`) 모두 타입 에러 발생
- **문제**: 런타임 에러 가능성, 자동 완성 미지원
- **영향도**: 테스트 신뢰성 감소

#### 6. **의존성 관리 복잡성**
- **수정 항목**: `package-lock.json` 584줄 증가
- **문제**: 최신 Playwright/Node 모듈 설치로 인한 잠금 파일 증가
- **리스크**: 번들 사이즈 증가, 설치 시간 지연

---

## 리팩토링 제안

### 1. **대용량 컴포넌트 분리 (우선순위: 높음)**

**`lib/claude.ts` (893줄) → 기능별 분리**
```
lib/
├── claude/
│   ├── ai-client.ts      (Claude API 통신)
│   ├── prompt-builder.ts (프롬프트 생성 로직)
│   ├── response-parser.ts (응답 파싱)
│   └── types.ts          (타입 정의)
```

**`components/InputSection.tsx` (824줄) → 컴포넌트 분해**
```
components/input/
├── InputSection.tsx      (컨테이너)
├── FileUploader.tsx
├── ParameterControl.tsx
├── PresetSelector.tsx
└── hooks/
    └── useInputState.ts
```

**`components/ReportTabs.tsx` (811줄) → 탭별 분리**
```
components/report/
├── ReportTabs.tsx        (탭 컨테이너)
├── tabs/
│   ├── OverviewTab.tsx
│   ├── DetailTab.tsx
│   └── ComparisonTab.tsx
```

### 2. **공통 타입 정의 통합**
```typescript
// types/index.ts (신규)
export type { Page, BrowserContext } from '@playwright/test';
export interface TestResult { /* ... */ }
export interface TestReport { /* ... */ }
```

### 3. **E2E 테스트 기본 설정 표준화**
```typescript
// e2e/fixtures.ts (신규)
import { test as base } from '@playwright/test';

type TestFixtures = {
  authenticatedPage: Page;
};

export const test = base.extend<TestFixtures>({
  authenticatedPage: async ({ page }, use) => {
    // 인증 처리
    await use(page);
  }
});
```

---

## 자동화 기회

### 1. **타입 체크 자동 수정 (우선순위: 긴급)**
```bash
# package.json scripts
"type-check:fix": "tsc --noEmit && npx eslint --fix 'e2e/**/*.ts'"
```

### 2. **CI/CD 파이프라인 개선**
```yaml
# .github/workflows/ci.yml 추가 단계
- name: TypeScript Check
  run: npm run type-check
  
- name: Auto-fix type errors
  run: npm run type-check:fix
  if: failure()
```

### 3. **의존성 버전 자동 검증**
```bash
# package.json scripts
"deps:validate": "npm list @playwright/test && tsc --noEmit",
"preinstall": "npm run deps:validate"
```

### 4. **대용량 파일 감시 자동화**
```bash
# scripts/check-file-size.ts (신규)
- 파일 크기 > 500줄 시 경고
- PR 체크로 자동 실행
- 리팩토링 제안 자동 생성
```

### 5. **E2E 테스트 커버리지 추적**
```bash
"test:e2e:report": "playwright test --reporter=html"
# CI에서 자동 생성 및 아카이빙
```

---

## 잠재적 버그 예측

### 1. **런타임 타입 에러 (확률: 높음)**
```typescript
// ❌ 위험: 암묵적 any 타입
test('settings', async ({ page }) => {
  // page 메서드 호출 시 IDE 지원 없음
  await page.click('#selector'); // 런타임 에러 가능
});
```

### 2. **모듈 해석 실패 → CI 파이프라인 중단 (확률: 중간)**
- 로컬 환경에서는 `npx playwright test` 작동하지만 Next.js 빌드 실패
- 배포 자동화 중단 예상

### 3. **컴포넌트 Props 타입 불일치**
- `InputSection.tsx` (824줄) 내부 서브컴포넌트가 Props를 명시적으로 정의하지 않으면
- 상위 컴포넌트 리팩토링 시 연쇄 에러 발생

### 4. **E2E 테스트 간헐적 실패**
```typescript
// 타입 미정의로 인한 가능한 이슈
// page.waitForSelector() 타입 부재 → 타임아웃 에러
// 재시도 로직 부재
```

### 5. **메모리 누수 (대용량 파일 시)**
- `lib/claude.ts` (893줄) 단일 파일 → 트리 쉐이킹 불가능
- 번들 크기 증가로 인한 성능 저하

---

## 권장 우선순위 (ACTION PLAN)

| 순위 | 작업 | 예상 시간 | 임팩트 |
|------|------|---------|--------|
| 1️⃣ | `@playwright/test` 모듈 설치 및 타입 정의 | 15분 | CRITICAL → PASS |
| 2️⃣ | E2E 테스트 파일 타입 명시 (e2e/*.spec.ts) | 30분 | HIGH → MEDIUM |
| 3️⃣ | `tsconfig.json` Playwright 통합 설정 | 20분 | CI 안정화 |
| 4️⃣ | `lib/claude.ts` 기능별 분리 | 4시간 | 유지보수성 ⬆️ |
| 5️⃣ | `components/InputSection.tsx` 분해 | 5시간 | 테스트 용이성 ⬆️ |
| 6️⃣ | E2E 테스트 고정 Fixture 작성 | 2시간 | 테스트 신뢰성 ⬆️ |

---

**작성일**: 2026-04-15  
**다음 점검**: 즉시 (CRITICAL 이슈 해결 후 72시간 내)  
**담당자**: Senior QA Engineer / Refactoring Specialist